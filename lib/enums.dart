enum SmartView { inbox, today, next7Days }

enum ViewMode { focus, list, calendar, settings }

enum FocusTab { time, pomo, stats }

enum FocusStatsRange { day, week, month }

enum CalendarViewMode { month, week, day }

enum TaskSortMode { date, priority, title }

enum DefaultStartPage { focus, tasks }

enum RepeatPreset { none, daily, weekly }
